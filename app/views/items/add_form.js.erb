$(function () {
  if ($('.js_item_form').length) {
    var categoryId = $('.js_item_form').find('#category_id').text();
    console.log(categoryId)
    $("<%= j(render 'items/form/add_item_form_btn', category_id: 0)%>").replaceAll($('.js_item_form')).attr('action', `/items/${categoryId}/add_form`);
  }

  $('#js_item_new_or_list_<%= @category_id %>').html("<%= j(render 'items/form/item_form', item: @item, items: @items, category_id: @category_id)%>");
});