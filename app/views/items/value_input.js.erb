var itemFormBox = $('#js_item_form_box');

itemFormBox.find('#category_id').val(`<%= @item.category_id %>`);
itemFormBox.find('#item_id').val(`<%= @item.id %>`);
itemFormBox.find('#name').val(`<%= @item.name %>`);
itemFormBox.find('#unit').val(`<%= @item.unit %>`);
